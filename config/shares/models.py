# from django.db import models
# from accounts.models import User
# from posts.models import Post
# from comments.models import Comment
# from profiles.models import Profile

# class Share(models.Model):

#     sender=models.ForeignKey(User,on_delete=models.CASCADE,null=True,blank=True,related_name="sender_shared")
#     receiver=models.ForeignKey(User,on_delete=models.CASCADE,null=True,blank=True,related_name="receiver_shared")
#     post=models.ForeignKey(Post,on_delete=models.CASCADE,null=True,blank=True,related_name="post_shared")
#     comment=models.ForeignKey(Comment,on_delete=models.CASCADE,null=True,blank=True,related_name="comment_shared")
#     profile=models.ForeignKey(Profile,on_delete=models.CASCADE,null=True,blank=True,related_name="profile_shared")
    
    
    
